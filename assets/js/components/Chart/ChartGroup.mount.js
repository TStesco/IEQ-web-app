'use strict';

const ChartGroup = require('ChartGroup.react');
const React = require('React');
const ReactDOM = require('ReactDOM');

ReactDOM.render(
  <ChartGroup />,
  document.getElementById('chartgroup-mount')
);
