'use strict';

const FAQWidget = require('FAQWidget.react');
const React = require('React');
const ReactDOM = require('ReactDOM');

ReactDOM.render(
  <FAQWidget />,
  document.getElementById('faq-widget')
);
