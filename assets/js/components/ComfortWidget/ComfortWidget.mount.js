'use strict';

const ComfortWidget = require('ComfortWidget.react');
const React = require('React');
const ReactDOM = require('ReactDOM');

ReactDOM.render(
  <ComfortWidget />,
  document.getElementById('comfort-mount')
);
