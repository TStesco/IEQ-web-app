'use strict';

const React = require('React');
const ReactDOM = require('ReactDOM');
const SidebarMenu = require('SidebarMenu.react');

ReactDOM.render(
  <SidebarMenu />,
  document.getElementById('sidebar-menu-list')
);
